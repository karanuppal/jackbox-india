{
  "name": "tamasha",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r --workspace-concurrency=1 test",
    "build": "pnpm -r build",
    "validate:content": "pnpm --filter @tamasha/shared exec vitest run test/content.test.ts"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  }
}
